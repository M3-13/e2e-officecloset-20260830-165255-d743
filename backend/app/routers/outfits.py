"""Outfit routes.

The routes themselves are implemented by the outfit-management ticket; this
skeleton only declares the router so the application can mount it under
``/api/outfits``.
"""

from fastapi import APIRouter

router = APIRouter()
