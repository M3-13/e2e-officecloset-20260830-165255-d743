"""Clothing-item routes.

The routes themselves are implemented by the item-management ticket; this
skeleton only declares the router so the application can mount it under
``/api/items``.
"""

from fastapi import APIRouter

router = APIRouter()
